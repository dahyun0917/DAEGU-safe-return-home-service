dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
        maven{ url "https://naver.jfrog.io/artifactory/maven/"}
    }
}
rootProject.name = "safe_return_home_service"
include ':app'
